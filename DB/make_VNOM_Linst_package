package LinstDB;


sub createEmpty {
my $pin = {
          };
bless $pin, 'LinstDB';
return $pin;
}# sub createEmpty



sub new {
my $pinRef = {
             C => "UND",
             };
bless $pinRef, 'LinstDB';
return $pinRef;
}#sub new

1;
