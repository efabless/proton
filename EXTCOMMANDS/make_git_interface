
sub clone_git {
my $url = $_[0];
use Git::Repository ;

#my $url = "https://github.com/collabchip/arduino.git";
#my $url = "https://github.com/collabchip/graywolf.git";
#my $dir = "test_code";
 #Git::Repository->run( clone => $url => $dir );
 Git::Repository->run( clone => $url);
 #$r = Git::Repository->new( work_tree => $dir );

}#sub clone_git

1;
